import { RootState } from "../..";

export const selectMovieDetails = (state: RootState) => state.movieDetails.movieDetails;
export const selectMovieDetailsStatus = (state: RootState) => state.movieDetails.movieDetails;